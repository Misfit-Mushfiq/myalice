name: myalice
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get:
  web_socket_channel: ^2.1.0
  pusher_client: ^1.1.3
  dio: ^4.0.0
  flutter_launcher_icons: ^0.9.0
  flutter_svg: ^0.22.0
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/launch_icon/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/launch_icon/myalice.png"
